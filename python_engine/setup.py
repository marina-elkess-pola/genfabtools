from setuptools import setup, find_packages

setup(
    name="parking_engine",
    version="0.5.0",
    packages=find_packages(),
    python_requires=">=3.8",
    description="GenFabTools Parking Engine - Feasibility-grade parking layout generation",
)
